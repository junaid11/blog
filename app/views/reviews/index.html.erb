<div class="container">
  <% @posts.each do |post| %>
    <h2>
      <%= post.title %>
    </h2>
    </a>
    <%= link_to 'Approve', review_path(post), method: :patch %>
    <%= link_to 'Destroy', post_path(post), method: :delete, data: { confirm: 'Are you sure?' } %>
    <hr>
  <% end %>
  <br/>
</div>
